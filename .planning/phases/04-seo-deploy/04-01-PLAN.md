---
phase: 04-seo-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/structured-data.ts
  - src/app/layout.tsx
  - src/app/galeri/page.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/opengraph-image.jpg
  - src/app/galeri/opengraph-image.jpg
autonomous: true
requirements: [SEOD-01, SEOD-02]

must_haves:
  truths:
    - "Sayfa kaynaginda JSON-LD application/ld+json script tagi HairSalon tipiyle mevcut"
    - "HTML head'de og:title, og:description, og:image, og:locale=tr_TR meta tagleri mevcut"
    - "Galeri sayfasinin ayri meta description ve og:image'i var"
    - "/sitemap.xml iki sayfa iceriyor (/ ve /galeri)"
    - "/robots.txt sitemap URL'sini iceriyor"
  artifacts:
    - path: "src/lib/structured-data.ts"
      provides: "JSON-LD generation function"
      exports: ["generateBusinessJsonLd"]
    - path: "src/app/sitemap.ts"
      provides: "Sitemap API"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Robots API"
      exports: ["default"]
    - path: "src/app/opengraph-image.jpg"
      provides: "Ana sayfa OG image (1200x630)"
    - path: "src/app/galeri/opengraph-image.jpg"
      provides: "Galeri sayfasi OG image (1200x630)"
  key_links:
    - from: "src/lib/structured-data.ts"
      to: "src/lib/constants.ts"
      via: "BUSINESS import"
      pattern: "import.*BUSINESS.*from.*constants"
    - from: "src/app/layout.tsx"
      to: "src/lib/structured-data.ts"
      via: "generateBusinessJsonLd call"
      pattern: "generateBusinessJsonLd"
    - from: "src/app/layout.tsx"
      to: "JSON-LD script tag"
      via: "dangerouslySetInnerHTML"
      pattern: "application/ld\\+json"
---

<objective>
JSON-LD structured data, Turkce meta/OG tagleri, sitemap, robots ve OG gorselleri ekleyerek siteyi arama motorlarina ve sosyal medya paylasimina optimize etmek.

Purpose: Yerel "susehri berber" aramalarinda Google'da gorunurluk ve sosyal paylasim onizlemeleri saglamak.
Output: structured-data.ts, genisletilmis layout.tsx metadata, sitemap.ts, robots.ts, OG gorseller.
</objective>

<execution_context>
@C:/Users/babai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/babai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seo-deploy/04-RESEARCH.md

<interfaces>
<!-- Business data source - all JSON-LD fields must come from here -->
From src/lib/constants.ts:
```typescript
export const BUSINESS = {
  name: "Salon Dejavu",
  subtitle: "Sac ve Sakal Tasarimi",
  phone: "05397256886",
  phoneFormatted: "0539 725 68 86",
  whatsappUrl: "https://wa.me/905397256886",
  address: "Cami Orta Mahallesi, Sivas Cd. No:55/C, 58600 Susehri/Sivas",
  hours: {
    weekdays: "07:00 - 23:30",
    sunday: "Kapali",
  },
  copyright: "...",
} as const;
```

From src/app/layout.tsx (current metadata):
```typescript
export const metadata: Metadata = {
  title: "Salon Dejavu — Sac ve Sakal Tasarimi",
  description: "Susehri/Sivas'taki Salon Dejavu Erkek Kuaforu...",
};
```

From src/app/galeri/page.tsx (current metadata):
```typescript
export const metadata: Metadata = {
  title: "Galeri — Salon Dejavu",
  description: "Salon Dejavu galeri - dukkan fotograflari ve calismalarimiz.",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON-LD structured data ve sitemap/robots dosyalari</name>
  <files>src/lib/structured-data.ts, src/app/sitemap.ts, src/app/robots.ts</files>
  <read_first>
    - src/lib/constants.ts
    - src/app/layout.tsx
    - .planning/phases/04-seo-deploy/04-RESEARCH.md
  </read_first>
  <action>
    1. Yeni dosya olustur: `src/lib/structured-data.ts`
       - `generateBusinessJsonLd(siteUrl: string)` fonksiyonu export et
       - `BUSINESS` objesini `@/lib/constants` den import et
       - Donus tipi: schema.org HairSalon JSON-LD objesi
       - Exact field'lar:
         ```
         "@context": "https://schema.org"
         "@type": "HairSalon"
         "name": BUSINESS.name  (= "Salon Dejavu")
         "alternateName": "Dejavu Erkek Kuaforu"
         "description": "Susehri/Sivas'taki Salon Dejavu Erkek Kuaforu. Profesyonel sac ve sakal tasarimi hizmetleri."
         "url": siteUrl parameter
         "telephone": "+905397256886"  (BUSINESS.phone'dan +90 prefix ekleyerek)
         "address": {
           "@type": "PostalAddress",
           "streetAddress": "Cami Orta Mahallesi, Sivas Cd. No:55/C",
           "addressLocality": "Susehri",
           "addressRegion": "Sivas",
           "postalCode": "58600",
           "addressCountry": "TR"
         }
         "geo": {
           "@type": "GeoCoordinates",
           "latitude": 40.163,
           "longitude": 38.087
         }
         "openingHoursSpecification": [{
           "@type": "OpeningHoursSpecification",
           "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
           "opens": "07:00",
           "closes": "23:30"
         }]
         "image": `${siteUrl}/images/gallery/dejavu-kuafor.jpg`
         "priceRange": "$$"
         "currenciesAccepted": "TRY"
         "paymentAccepted": "Cash, Credit Card"
         ```

    2. Yeni dosya olustur: `src/app/sitemap.ts`
       - `export default function sitemap(): MetadataRoute.Sitemap` formatinda
       - `const baseUrl = "https://dejavu-barber.vercel.app"`
       - 2 sayfa: `{ url: baseUrl, lastModified: new Date(), changeFrequency: "monthly", priority: 1 }` ve `{ url: baseUrl + "/galeri", lastModified: new Date(), changeFrequency: "monthly", priority: 0.8 }`

    3. Yeni dosya olustur: `src/app/robots.ts`
       - `export default function robots(): MetadataRoute.Robots` formatinda
       - Rules: `{ userAgent: "*", allow: "/" }`
       - Sitemap: `"https://dejavu-barber.vercel.app/sitemap.xml"`
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/structured-data.ts contains `export function generateBusinessJsonLd`
    - src/lib/structured-data.ts contains `"@type": "HairSalon"`
    - src/lib/structured-data.ts contains `import { BUSINESS } from`
    - src/lib/structured-data.ts contains `latitude: 40.163`
    - src/lib/structured-data.ts contains `"opens": "07:00"`
    - src/app/sitemap.ts contains `export default function sitemap`
    - src/app/sitemap.ts contains `dejavu-barber.vercel.app`
    - src/app/robots.ts contains `export default function robots`
    - src/app/robots.ts contains `sitemap.xml`
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>structured-data.ts HairSalon JSON-LD fonksiyonu export ediyor, sitemap.ts 2 sayfa iceriyor, robots.ts sitemap referansli</done>
</task>

<task type="auto">
  <name>Task 2: Layout/galeri metadata genisletme, JSON-LD enjeksiyonu ve OG gorselleri</name>
  <files>src/app/layout.tsx, src/app/galeri/page.tsx, src/app/opengraph-image.jpg, src/app/galeri/opengraph-image.jpg</files>
  <read_first>
    - src/app/layout.tsx
    - src/app/galeri/page.tsx
    - src/lib/structured-data.ts
    - src/lib/constants.ts
    - .planning/phases/04-seo-deploy/04-CONTEXT.md
  </read_first>
  <action>
    1. `src/app/layout.tsx` metadata objesini genislet:
       - `metadataBase: new URL("https://dejavu-barber.vercel.app")` ekle
       - `title` obje formatina cevir: `{ default: "Salon Dejavu — Susehri Berber | Sac ve Sakal Tasarimi", template: "%s | Salon Dejavu" }`
       - `description`: `"Susehri'nin en iyi erkek kuaforu. Profesyonel sac kesimi, sakal trasi, cilt bakimi. Salon Dejavu — Sivas Cd. No:55/C, Susehri/Sivas. Randevu icin: 0539 725 68 86"`
       - `keywords`: `["susehri berber", "Susehri berber", "Suşehri berber", "susehri erkek kuaforu", "Suşehri erkek kuaförü", "sivas berber", "Sivas berber", "salon dejavu", "Salon Dejavu"]`
       - `authors`: `[{ name: "Salon Dejavu" }]`
       - `openGraph`: `{ title: "Salon Dejavu — Susehri Berber", description: "Susehri'nin en iyi erkek kuaforu. Profesyonel sac ve sakal tasarimi.", url: "https://dejavu-barber.vercel.app", siteName: "Salon Dejavu", locale: "tr_TR", type: "website" }`
       - `alternates`: `{ canonical: "https://dejavu-barber.vercel.app" }`
       - `robots`: `{ index: true, follow: true }`

    2. `src/app/layout.tsx` RootLayout body icine JSON-LD script tagi ekle:
       - `import { generateBusinessJsonLd } from "@/lib/structured-data"` ekle
       - RootLayout fonksiyonu icinde: `const jsonLd = generateBusinessJsonLd("https://dejavu-barber.vercel.app")`
       - `<body>` taginin hemen icinde, AnimationProvider'dan ONCE:
         ```
         <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, "\\u003c") }} />
         ```

    3. `src/app/layout.tsx`'a Vercel Analytics ekle:
       - `import { Analytics } from "@vercel/analytics/next"` ekle
       - `<Analytics />` komponentini `</body>` taginden hemen once, WhatsAppButton'dan sonra ekle
       - NOT: @vercel/analytics henuz yuklenmemisse once `npm install @vercel/analytics` calistirilmali

    4. `src/app/galeri/page.tsx` metadata objesini genislet:
       - `title`: `"Galeri"` (template sayesinde "Galeri | Salon Dejavu" olacak)
       - `description`: `"Salon Dejavu galeri — dukkan fotograflari, sac tasarimi calismalari ve berber salonu ic mekan goruntuler. Susehri/Sivas."`
       - `openGraph`: `{ title: "Galeri — Salon Dejavu", description: "Salon Dejavu galeri — dukkan fotograflari ve sac tasarimi calismalari.", url: "https://dejavu-barber.vercel.app/galeri" }`
       - `alternates`: `{ canonical: "https://dejavu-barber.vercel.app/galeri" }`

    5. OG gorselleri olustur (1200x630, JPEG):
       - Ana sayfa: `resimler/dejavu-kuafor.jpg` -> `src/app/opengraph-image.jpg` (dukkan disi, neon tabela)
       - Galeri: `resimler/kuafor-icerisi.jpg` -> `src/app/galeri/opengraph-image.jpg` (ic mekan)
       - Sharp kullan (npx veya script ile): `npx sharp -i resimler/dejavu-kuafor.jpg -o src/app/opengraph-image.jpg resize 1200 630 --fit cover`
       - Alternatif: Node.js script ile sharp API kullanarak resize yap
       - Eger sharp CLI calismiyorsa, basit bir Node.js script yazarak sharp'in programmatic API'sini kullan:
         ```javascript
         const sharp = require('sharp');
         sharp('resimler/dejavu-kuafor.jpg').resize(1200, 630, { fit: 'cover' }).jpeg({ quality: 85 }).toFile('src/app/opengraph-image.jpg');
         sharp('resimler/kuafor-icerisi.jpg').resize(1200, 630, { fit: 'cover' }).jpeg({ quality: 85 }).toFile('src/app/galeri/opengraph-image.jpg');
         ```
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `metadataBase: new URL("https://dejavu-barber.vercel.app")`
    - src/app/layout.tsx contains `locale: "tr_TR"`
    - src/app/layout.tsx contains `"susehri berber"`
    - src/app/layout.tsx contains `application/ld+json`
    - src/app/layout.tsx contains `generateBusinessJsonLd`
    - src/app/layout.tsx contains `import { Analytics } from "@vercel/analytics/next"`
    - src/app/layout.tsx contains `<Analytics />`
    - src/app/layout.tsx contains `\\u003c` (XSS protection in JSON-LD stringify)
    - src/app/galeri/page.tsx contains `canonical: "https://dejavu-barber.vercel.app/galeri"`
    - src/app/galeri/page.tsx contains `openGraph`
    - src/app/opengraph-image.jpg exists and is > 10KB
    - src/app/galeri/opengraph-image.jpg exists and is > 10KB
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Layout.tsx JSON-LD + OG + keywords + canonical iceriyor, galeri ayri metadata'ya sahip, OG gorselleri 1200x630 mevcut, Analytics komponenti eklenmis, build basarili</done>
</task>

</tasks>

<verification>
- `npx next build` hatasiz tamamlanir
- Build ciktisinda tum rotalar (/, /galeri) listelenir
- src/lib/structured-data.ts HairSalon JSON-LD export eder
- layout.tsx JSON-LD script tagi, genisletilmis metadata ve Analytics iceriyor
- sitemap.ts ve robots.ts dosyalari mevcut
- OG gorselleri her iki rota icin mevcut
</verification>

<success_criteria>
- JSON-LD script tagi HairSalon tipiyle sayfa kaynaginda gorunur
- Meta taglerde og:title, og:description, og:locale=tr_TR mevcut
- Galeri sayfasinin kendine ait og:image ve meta description'i var
- /sitemap.xml 2 URL iceriyor
- /robots.txt sitemap referansi iceriyor
- Build hatasiz tamamlaniyor
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-deploy/04-01-SUMMARY.md`
</output>
