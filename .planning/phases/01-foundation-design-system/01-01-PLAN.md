---
phase: 01-foundation-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/constants.ts
  - src/components/layout/SectionDivider.tsx
  - src/components/ui/GrainOverlay.tsx
  - src/components/ui/RetroCard.tsx
  - next.config.ts
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04
  - VISL-01
  - VISL-02
  - VISL-03

must_haves:
  truths:
    - "npm run dev baslatildiginda Next.js 15 App Router sayfasi localhost:3000'de acilir"
    - "Sayfa koyu kahverengi (#1C1410) arka plan ile yuklenir"
    - "Playfair Display ve Inter fontlari yuklenir ve uygulanir"
    - "Grain overlay sayfa uzerinde gorunur (opacity ~4%)"
    - "Barber pole divider kirmizi-beyaz-mavi cizgili gorunur"
    - "Retro card altin kose suslemeleriyle gorunur"
    - "Neon glow CSS class'lari kirmizi isik efekti verir"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens, neon glow classes, grain overlay, retro card, barber pole"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, metadata, body classes"
      contains: "Playfair_Display"
    - path: "src/app/page.tsx"
      provides: "Demo page showing design system components"
      contains: "SectionDivider"
    - path: "src/lib/constants.ts"
      provides: "Business data and nav items"
      exports: ["NAV_ITEMS", "BUSINESS"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "Tailwind @theme tokens consumed by utility classes"
      pattern: "font-body|bg-base-dark"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Font CSS variables set on html element"
      pattern: "playfair\\.variable.*inter\\.variable"
---

<objective>
Next.js 15 proje kurulumu ve vintage Turk berber tasarim sistemi.

Purpose: Projenin temelini atan ilk plan. Tum sonraki planlarin ustune insa edecegi Next.js projesi, Tailwind v4 design token'lari, font'lar ve gorsel motifler (grain, neon glow, barber pole, retro card) burada olusturulur.
Output: Calisir Next.js 15 App Router projesi, tam design system CSS, ornek demo sayfa.
</objective>

<execution_context>
@C:/Users/babai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/babai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 15 proje kurulumu + Tailwind v4 design tokens + font'lar</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, src/lib/constants.ts</files>
  <read_first>
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Pattern 5: Tailwind v4 @theme, Pattern 6: next/font, Standard Stack section for exact versions)
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Color section, Typography section, Spacing Scale section, Layout Contract section, Copywriting Contract section)
    .planning/phases/01-foundation-design-system/01-CONTEXT.md (Locked decisions)
  </read_first>
  <action>
1. Proje olustur:
   ```bash
   npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
   ```
   Not: Proje root'ta (D:\nextJS\berber-sitesi) kurulacak. create-next-app bos olmayan klasorde calismayabilir, bu durumda gecici bir klasorde olusturup dosyalari tasi.

2. Animasyon paketlerini kur (sonraki plan icin hazir olsun):
   ```bash
   npm install gsap @gsap/react motion lenis
   ```

3. `src/app/globals.css` — Tailwind v4 @theme ile tum design token'lari tanimla:
   ```css
   @import "tailwindcss";

   @theme {
     --color-base-dark: #1C1410;
     --color-base-medium: #211812;
     --color-base-light: #150F0B;
     --color-neon-red: #E11D48;
     --color-gold: #D4A853;
     --color-cream: #F5E6D0;
     --color-white: #FAFAF9;

     --shadow-neon: 0 0 10px #E11D48, 0 0 40px rgba(225, 29, 72, 0.3);
     --shadow-neon-strong: 0 0 10px #E11D48, 0 0 40px #E11D48, 0 0 80px rgba(225, 29, 72, 0.2);

     --font-display: var(--font-playfair);
     --font-body: var(--font-inter);

     --spacing-section: 6rem;
     --spacing-section-mobile: 3rem;

     --container-content: 1200px;
   }

   @theme inline {
     --font-display: var(--font-playfair);
     --font-body: var(--font-inter);
   }
   ```

   Ayni dosyaya utility class'lari ekle:

   ```css
   /* Neon Glow Text */
   .neon-glow {
     text-shadow:
       0 0 7px #E11D48,
       0 0 10px #E11D48,
       0 0 21px #E11D48,
       0 0 42px rgba(225, 29, 72, 0.5);
   }
   .neon-glow-subtle {
     text-shadow:
       0 0 5px rgba(225, 29, 72, 0.5),
       0 0 15px rgba(225, 29, 72, 0.2);
   }

   /* Grain Overlay */
   .grain-overlay::before {
     content: "";
     position: fixed;
     inset: 0;
     z-index: 50;
     pointer-events: none;
     opacity: 0.04;
     background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
   }

   /* Retro Card */
   .retro-card {
     position: relative;
     border: 1px solid rgba(212, 168, 83, 0.3);
   }
   .retro-card::before,
   .retro-card::after {
     content: "";
     position: absolute;
     width: 20px;
     height: 20px;
     border-color: #D4A853;
   }
   .retro-card::before {
     top: -1px;
     left: -1px;
     border-top: 2px solid;
     border-left: 2px solid;
   }
   .retro-card::after {
     bottom: -1px;
     right: -1px;
     border-bottom: 2px solid;
     border-right: 2px solid;
   }

   /* Barber Pole Animation */
   @keyframes barber-slide {
     from { transform: translateX(0); }
     to { transform: translateX(-50%); }
   }
   .animate-barber-slide {
     animation: barber-slide 3s linear infinite;
   }
   ```

4. `src/app/layout.tsx` — Root layout:
   - Import Playfair_Display ve Inter from "next/font/google"
   - Playfair_Display: subsets ["latin"], variable "--font-playfair", display "swap", weight ["700"]
   - Inter: subsets ["latin"], variable "--font-inter", display "swap", weight ["400", "700"]
   - html lang="tr", className `${playfair.variable} ${inter.variable}`
   - body className: "font-body bg-base-dark text-cream min-h-screen"
   - Body icine `<div className="grain-overlay">{children}</div>` (grain overlay wrapper)
   - Metadata: title "Salon Dejavu — Sac ve Sakal Tasarimi", description "Susehri/Sivas'taki Salon Dejavu Erkek Kuaforu..."

5. `src/lib/constants.ts` — Isletme verileri:
   ```typescript
   export const BUSINESS = {
     name: "Salon Dejavu",
     subtitle: "Sac ve Sakal Tasarimi",
     phone: "05397256886",
     phoneFormatted: "0539 725 68 86",
     whatsappUrl: "https://wa.me/905397256886",
     address: "Cami Orta Mahallesi, Sivas Cd. No:55/C, 58600 Susehri/Sivas",
     hours: {
       weekdays: "07:00 - 23:30",
       sunday: "Kapali",
     },
     copyright: "© 2026 Salon Dejavu — Tum haklari saklidir.",
   } as const;

   export const NAV_ITEMS = [
     { label: "Hakkimizda", href: "#hakkimizda" },
     { label: "Hizmetler", href: "#hizmetler" },
     { label: "Galeri", href: "/galeri" },
     { label: "Iletisim", href: "#iletisim" },
   ] as const;

   export const CTA_TEXT = "RANDEVU AL" as const;
   ```

6. `src/app/page.tsx` — Gecici demo sayfa (design system showcase):
   - SectionDivider, GrainOverlay, RetroCard componentlerini import et
   - "DEJAVU" basligini font-display, text-[64px] (desktop) / text-[40px] (mobil), font-bold, neon-glow class ile goster
   - "Sac ve Sakal Tasarimi" alt baslik text-cream
   - Ornek bolumler: alternating bg (bg-base-dark, bg-base-medium, bg-base-light)
   - Her bolum arasi SectionDivider component
   - Bir adet RetroCard ornegi altin kose suslemeleriyle
   - Copyright satiri en altta: BUSINESS.copyright, text-sm (14px Label), text-center
  </action>
  <verify>
    <automated>cd D:/nextJS/berber-sitesi && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" dependency
    - package.json contains "gsap" and "@gsap/react" and "motion" and "lenis" dependencies
    - src/app/globals.css contains "@theme" and "--color-base-dark: #1C1410" and "--color-neon-red: #E11D48" and "--color-gold: #D4A853"
    - src/app/globals.css contains ".neon-glow" and ".grain-overlay::before" and ".retro-card" and "@keyframes barber-slide"
    - src/app/layout.tsx contains "Playfair_Display" and "Inter" and 'lang="tr"' and "font-body" and "bg-base-dark"
    - src/app/layout.tsx contains "--font-playfair" and "--font-inter"
    - src/lib/constants.ts contains "NAV_ITEMS" and "BUSINESS" and "05397256886" and "RANDEVU AL"
    - src/app/page.tsx contains "DEJAVU" and "neon-glow" and "SectionDivider" and "RetroCard"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Next.js 15 projesi build ediliyor, Tailwind v4 design token'lari tanimli, fontlar yuklu, demo sayfada design system gorsel motifleri gorunur.</done>
</task>

<task type="auto">
  <name>Task 2: GrainOverlay, SectionDivider ve RetroCard componentleri</name>
  <files>src/components/ui/GrainOverlay.tsx, src/components/layout/SectionDivider.tsx, src/components/ui/RetroCard.tsx</files>
  <read_first>
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Visual Motifs section — grain, barber pole, retro card exact specs)
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Barber Pole Divider code example, Retro Card Frame code example, Grain Overlay code example)
    src/app/globals.css (CSS class definitions already created in Task 1)
  </read_first>
  <action>
1. `src/components/ui/GrainOverlay.tsx`:
   ```typescript
   // "use client" is NOT needed — this is a pure CSS component, no JS interactivity
   export function GrainOverlay() {
     return <div className="grain-overlay" aria-hidden="true" />;
   }
   ```
   CSS class ".grain-overlay::before" zaten globals.css'de tanimli (Task 1).
   Component sadece div wrapper'i saglar. aria-hidden="true" dekoratif eleman icin.

2. `src/components/layout/SectionDivider.tsx`:
   ```typescript
   export function SectionDivider() {
     return (
       <div className="h-2 w-full overflow-hidden" aria-hidden="true">
         <div
           className="h-full w-[200%] animate-barber-slide"
           style={{
             background:
               "repeating-linear-gradient(120deg, #E11D48 0px, #E11D48 20px, #FAFAF9 20px, #FAFAF9 40px, #3B82F6 40px, #3B82F6 60px)",
           }}
         />
       </div>
     );
   }
   ```
   Height: 8px (h-2). Stripe width: 20px per color. Angle: 120deg.
   Colors: #E11D48 (neon-red), #FAFAF9 (white), #3B82F6 (blue).
   Animation: barber-slide keyframe (globals.css'de tanimli).

3. `src/components/ui/RetroCard.tsx`:
   ```typescript
   export function RetroCard({
     children,
     className = "",
   }: {
     children: React.ReactNode;
     className?: string;
   }) {
     return (
       <div className={`retro-card p-6 ${className}`}>
         {children}
       </div>
     );
   }
   ```
   CSS class ".retro-card" globals.css'de tanimli — altin kose suslemeleri (::before top-left, ::after bottom-right), 1px solid rgba(212, 168, 83, 0.3) border.
   p-6 (24px) default padding.
  </action>
  <verify>
    <automated>cd D:/nextJS/berber-sitesi && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/GrainOverlay.tsx contains "grain-overlay" and 'aria-hidden="true"'
    - src/components/layout/SectionDivider.tsx contains "animate-barber-slide" and "repeating-linear-gradient" and "#E11D48" and "#FAFAF9" and "#3B82F6"
    - src/components/ui/RetroCard.tsx contains "retro-card" and "children" and "className"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Uc gorsel motif componenti olusturuldu: GrainOverlay (grain doku), SectionDivider (berber diregi cizgileri), RetroCard (retro kose suslemeli kart). Hepsi TypeScript hatasiz compile oluyor.</done>
</task>

</tasks>

<verification>
- `npm run build` hatasiz tamamlaniyor
- `npx tsc --noEmit` TypeScript hatalari yok
- `npm run dev` ile localhost:3000'de sayfa acildiginda:
  - Koyu kahverengi (#1C1410) arka plan gorunur
  - DEJAVU basligi neon glow ile gorunur
  - Grain overlay fark edilir (hafif noise dokusu)
  - Barber pole divider kirmizi-beyaz-mavi cizgili animasyonlu
  - RetroCard altin kose suslemeleriyle gorunur
  - Alternating section background'lar gorunur
</verification>

<success_criteria>
- Working Next.js 15 App Router projesi (build + dev basarili)
- Tailwind v4 @theme ile 7 renk token, 2 shadow token, 2 font token, spacing token tanimli
- Playfair Display (serif) + Inter (sans-serif) font'lari yuklu ve CSS variable olarak erisilebilir
- 3 gorsel motif componenti: GrainOverlay, SectionDivider, RetroCard
- Demo sayfa design system elementlerini sergiliyor
- Isletme verileri constants.ts'de merkezi
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-01-SUMMARY.md`
</output>
