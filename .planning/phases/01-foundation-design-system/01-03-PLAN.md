---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/providers/SmoothScrollProvider.tsx
  - src/hooks/useScrollSpy.ts
  - src/components/layout/Navbar.tsx
  - src/components/layout/MobileMenu.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "Desktop'ta Lenis smooth scroll aktif, scrollbar hareket yumusak"
    - "Mobilde Lenis devre disi, native scroll calisiyor"
    - "Navbar hero uzerinde transparent, scroll edince koyu arka plan + blur ile sticky"
    - "Navbar'daki aktif bolum neon kirmizi alt cizgi ile isaretli (scroll-spy)"
    - "Navbar linkine tiklandiginda ilgili bolume smooth scroll ile gidiliyor"
    - "Mobilde hamburger menusune tiklandiginda tam ekran overlay aciliyor"
    - "Mobil menude Randevu Al butonu gorunuyor"
  artifacts:
    - path: "src/components/providers/SmoothScrollProvider.tsx"
      provides: "Desktop-only Lenis + GSAP ticker sync"
      contains: "ReactLenis"
    - path: "src/hooks/useScrollSpy.ts"
      provides: "IntersectionObserver-based active section tracking"
      exports: ["useScrollSpy"]
    - path: "src/components/layout/Navbar.tsx"
      provides: "Sticky transparent-to-solid navbar with scroll-spy"
      contains: "DEJAVU"
    - path: "src/components/layout/MobileMenu.tsx"
      provides: "Full-screen overlay mobile menu"
      contains: "AnimatePresence"
  key_links:
    - from: "src/components/providers/SmoothScrollProvider.tsx"
      to: "src/lib/gsap-config.ts"
      via: "GSAP ticker sync for Lenis"
      pattern: "gsap\\.ticker\\.add"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/hooks/useScrollSpy.ts"
      via: "Active section highlighting"
      pattern: "useScrollSpy"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/lib/constants.ts"
      via: "Navigation items"
      pattern: "NAV_ITEMS"
    - from: "src/components/layout/MobileMenu.tsx"
      to: "motion/react"
      via: "AnimatePresence enter/exit"
      pattern: "AnimatePresence"
---

<objective>
Lenis smooth scroll, sticky navbar ve scroll-spy navigasyonu.

Purpose: Kullanici deneyiminin temel navigasyon katmani. Desktop'ta butter-smooth scroll, transparent->solid navbar gecisi, aktif bolumu gosteren neon scroll-spy ve tam ekran mobil menu. Bunlar Phase 2 icerik bolumleri icin hazir altyapi.
Output: SmoothScrollProvider, Navbar, MobileMenu, useScrollSpy hook, ornek bolumlerle tam demo.
</objective>

<execution_context>
@C:/Users/babai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/babai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-UI-SPEC.md

@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 + 02 — executor needs these -->

From src/lib/gsap-config.ts:
```typescript
export { gsap, ScrollTrigger, useGSAP };
```

From src/hooks/useAnimationTier.ts:
```typescript
export type AnimationTier = "full" | "reduced" | "none";
export function useAnimationTier(): AnimationTier;
```

From src/lib/constants.ts:
```typescript
export const NAV_ITEMS = [
  { label: "Hakkimizda", href: "#hakkimizda" },
  { label: "Hizmetler", href: "#hizmetler" },
  { label: "Galeri", href: "/galeri" },
  { label: "Iletisim", href: "#iletisim" },
] as const;
export const CTA_TEXT = "RANDEVU AL" as const;
export const BUSINESS = { name: "Salon Dejavu", whatsappUrl: "https://wa.me/905397256886", ... } as const;
```

Design tokens (Tailwind utilities from globals.css):
- bg-base-dark, bg-base-medium, bg-base-light
- text-neon-red, text-gold, text-cream, text-white
- shadow-neon, shadow-neon-strong
- font-display, font-body
- .neon-glow, .neon-glow-subtle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SmoothScrollProvider + useScrollSpy hook</name>
  <files>src/components/providers/SmoothScrollProvider.tsx, src/hooks/useScrollSpy.ts, src/app/layout.tsx</files>
  <read_first>
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Pattern 3: Lenis + GSAP Ticker Sync, Desktop-Only Lenis Provider code example, Pitfall 2: Lenis + ScrollTrigger Desync, Pitfall 5: Lenis Infinite Re-render)
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Animation Tier Contract — Lenis desktop only, Navbar Specification — scroll-spy)
    .planning/phases/01-foundation-design-system/01-CONTEXT.md (Smooth scroll locked decisions — Lenis sadece desktop, mobilde native)
    src/app/layout.tsx (current layout to add SmoothScrollProvider)
    src/hooks/useAnimationTier.ts (useAnimationTier signature for tier detection)
    src/lib/gsap-config.ts (gsap, ScrollTrigger exports for ticker sync)
  </read_first>
  <action>
1. `src/components/providers/SmoothScrollProvider.tsx`:
   ```typescript
   "use client";
   import { ReactLenis } from "lenis/react";
   import { useEffect, useRef } from "react";
   import { gsap, ScrollTrigger } from "@/lib/gsap-config";
   import { useAnimationTier } from "@/hooks/useAnimationTier";

   export function SmoothScrollProvider({ children }: { children: React.ReactNode }) {
     const lenisRef = useRef<any>(null);
     const tier = useAnimationTier();
     const isDesktop = tier === "full";

     useEffect(() => {
       if (!isDesktop) return;

       function update(time: number) {
         lenisRef.current?.lenis?.raf(time * 1000);
       }
       gsap.ticker.add(update);
       gsap.ticker.lagSmoothing(0);

       const lenis = lenisRef.current?.lenis;
       if (lenis) {
         lenis.on("scroll", ScrollTrigger.update);
       }

       return () => {
         gsap.ticker.remove(update);
       };
     }, [isDesktop]);

     if (!isDesktop) {
       return <>{children}</>;
     }

     return (
       <ReactLenis root options={{ autoRaf: false, lerp: 0.1 }} ref={lenisRef}>
         {children}
       </ReactLenis>
     );
   }
   ```
   KRITIK: `autoRaf: false` — Lenis kendi RAF loop'unu BASLATMAMALI, GSAP ticker'dan surulmeli.
   KRITIK: `gsap.ticker.lagSmoothing(0)` — smooth scroll icin gerekli.
   KRITIK: `lenis.on("scroll", ScrollTrigger.update)` — ScrollTrigger senkronizasyonu.
   KRITIK: Mobilde (tier !== "full") Lenis DEVRE DISI — sadece children render edilir.
   KRITIK: useEffect dependency [isDesktop] — tier degistiginde cleanup ve re-setup.
   PITFALL: Options objesini useRef veya const ile sakla, useEffect dependency'ye KOYMA (infinite re-render).

2. `src/hooks/useScrollSpy.ts`:
   ```typescript
   "use client";
   import { useState, useEffect } from "react";

   export function useScrollSpy(sectionIds: string[], offset: number = 100): string | null {
     const [activeId, setActiveId] = useState<string | null>(null);

     useEffect(() => {
       const observer = new IntersectionObserver(
         (entries) => {
           // En ust goruntudeki section'i bul
           const visibleEntries = entries.filter((e) => e.isIntersecting);
           if (visibleEntries.length > 0) {
             // En yakin olan (en yuksek intersectionRatio)
             const closest = visibleEntries.reduce((prev, curr) =>
               curr.intersectionRatio > prev.intersectionRatio ? curr : prev
             );
             setActiveId(closest.target.id);
           }
         },
         {
           rootMargin: `-${offset}px 0px -50% 0px`,
           threshold: [0, 0.25, 0.5, 0.75, 1],
         }
       );

       sectionIds.forEach((id) => {
         const el = document.getElementById(id);
         if (el) observer.observe(el);
       });

       return () => observer.disconnect();
     }, [sectionIds, offset]);

     return activeId;
   }
   ```
   rootMargin: Ust taraftan offset px (navbar yuksekligi icin), alt taraftan %50 — sayfanin ust yarisinda olan section aktif.
   threshold: Birden fazla esik ile daha hassas tespit.

3. `src/app/layout.tsx` GUNCELLE — SmoothScrollProvider ekle (Navbar HENUZ EKLENMEYECEK):
   AnimationProvider icine SmoothScrollProvider ekle. Navbar bu task'ta EKLENMEMELI — Navbar.tsx henuz mevcut degil ve import edilirse tsc --noEmit hata verir. Navbar eklenmesi Task 2'ye ait.
   ```tsx
   <body className="font-body bg-base-dark text-cream min-h-screen">
     <AnimationProvider>
       <SmoothScrollProvider>
         <div className="grain-overlay">
           {children}
         </div>
       </SmoothScrollProvider>
     </AnimationProvider>
   </body>
   ```
   NOT: Navbar, SmoothScrollProvider ICINDE olmali ki Lenis scroll event'lerini alabilsin. Bu yapi Task 2'de Navbar olusturulduktan sonra saglanacak.
  </action>
  <verify>
    <automated>cd D:/nextJS/berber-sitesi && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/providers/SmoothScrollProvider.tsx contains '"use client"' and "ReactLenis" and "autoRaf: false" and "gsap.ticker.add" and "gsap.ticker.lagSmoothing(0)" and 'lenis.on("scroll", ScrollTrigger.update)' and 'tier === "full"'
    - src/hooks/useScrollSpy.ts contains "IntersectionObserver" and "setActiveId" and "rootMargin"
    - src/app/layout.tsx contains "SmoothScrollProvider" and "AnimationProvider"
    - src/app/layout.tsx does NOT contain "Navbar" (Navbar is added in Task 2)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Lenis smooth scroll desktop'ta aktif (GSAP ticker senkronize), mobilde devre disi. useScrollSpy IntersectionObserver ile aktif bolumu takip ediyor. Layout SmoothScrollProvider ile sarili, Navbar henuz eklenmedi (Task 2'de eklenecek).</done>
</task>

<task type="auto">
  <name>Task 2: Navbar + MobileMenu + demo sections ile tam scroll deneyimi</name>
  <files>src/components/layout/Navbar.tsx, src/components/layout/MobileMenu.tsx, src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Navbar Specification — TAM tablo, her property: desktop/mobil, height, transition, logo, items, CTA, active indicator, mobile trigger, mobile menu)
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Accessibility Contract — aria-label for hamburger, close button)
    .planning/phases/01-foundation-design-system/01-UI-SPEC.md (Copywriting Contract — navbar items text, CTA text)
    .planning/phases/01-foundation-design-system/01-CONTEXT.md (Navbar & layout locked decisions)
    .planning/phases/01-foundation-design-system/01-RESEARCH.md (Anti-Patterns — GSAP + Motion ayni elemanda YASAK)
    src/lib/constants.ts (NAV_ITEMS, CTA_TEXT, BUSINESS exports)
    src/hooks/useScrollSpy.ts (useScrollSpy signature)
    src/hooks/useAnimationTier.ts (useAnimationTier signature)
    src/components/providers/SmoothScrollProvider.tsx (Lenis context)
  </read_first>
  <action>
1. `src/components/layout/Navbar.tsx`:
   ```typescript
   "use client";
   ```
   Motion-driven component (GSAP degil — CONTEXT.md locked: Motion sorumluluk = navbar gecisi).

   Props: Yok (global component).

   State:
   - `scrolled` (boolean): scroll > 50px ise true (transparent -> solid transition tetikler)
   - `mobileMenuOpen` (boolean): hamburger menu state
   - `activeSection`: useScrollSpy(["hakkimizda", "hizmetler", "iletisim"]) — "galeri" HARIC (ayri sayfa)

   Scroll detection:
   ```typescript
   useEffect(() => {
     function onScroll() { setScrolled(window.scrollY > 50); }
     window.addEventListener("scroll", onScroll, { passive: true });
     return () => window.removeEventListener("scroll", onScroll);
   }, []);
   ```

   Desktop layout (md: ve ustu):
   - Fixed, top-0, w-full, z-40
   - Height: h-16 (64px — 3xl token)
   - Background: scrolled ? "bg-base-dark/95 backdrop-blur-md border-b border-gold/10" : "bg-transparent"
   - Transition: Motion `motion.nav` ile animate={{ backgroundColor, borderColor }}
   - Sol: "DEJAVU" in font-display, text-lg (veya uygun boyut), text-white, neon-glow class
   - Orta: NAV_ITEMS.map ile linkler — text-sm (14px), font-bold, uppercase, tracking-wider, text-cream
   - Aktif link: altinda 2px neon-red bar + neon glow. `activeSection === item.href.slice(1)` kontrolu.
     ```tsx
     {activeSection === sectionId && (
       <motion.div
         layoutId="nav-indicator"
         className="absolute -bottom-px left-0 right-0 h-0.5 bg-neon-red"
         style={{ boxShadow: "var(--shadow-neon)" }}
       />
     )}
     ```
   - Sag: CTA_TEXT butonu — bg-neon-red, text-white, text-sm, font-bold, uppercase, tracking-wider, px-4 py-2, rounded
     href={BUSINESS.whatsappUrl}, target="_blank", rel="noopener noreferrer"

   Mobil layout (md: altinda):
   - Height: h-12 (48px — 2xl token)
   - Sol: "DEJAVU" logo (kucuk)
   - Sag: Hamburger butonu — 44x44px touch target, aria-label="Menu", aria-expanded={mobileMenuOpen}
   - NAV_ITEMS ve CTA GIZLI — MobileMenu'de gorunur

   Link click handler:
   - Hash link (#hakkimizda vb.): `e.preventDefault()`, smooth scroll to element, close mobile menu
   - Absolute link (/galeri): normal navigasyon

2. `src/components/layout/MobileMenu.tsx`:
   ```typescript
   "use client";
   ```
   Motion AnimatePresence ile enter/exit animasyonlu tam ekran overlay.

   Props: `isOpen: boolean`, `onClose: () => void`

   Layout:
   - Fixed, inset-0, z-50, bg-base-dark
   - AnimatePresence + motion.div: initial={{ opacity: 0 }}, animate={{ opacity: 1 }}, exit={{ opacity: 0 }}
   - Ust sag: Kapat butonu — aria-label="Menuyu kapat", 44x44px touch target
   - Ortada: NAV_ITEMS + CTA dikey listesi, her biri motion.div ile stagger animasyonlu
   - Her link: text-base (16px Body bold — UI-SPEC'te belirtildi), font-bold, text-cream, py-3 (44px+ touch target)
   - Aktif link: sol kenar neon-red border (border-l-2 border-neon-red)
   - En altta: CTA_TEXT butonu — bg-neon-red, text-white, font-bold, uppercase, tracking-wider, w-full, py-3

   Link click: Call onClose(), smooth scroll to section.

3. `src/app/page.tsx` GUNCELLE — Scroll demo icin section'lar ekle:
   Mevcut demo icerigin UZERINE, scroll-spy test icin ornek section'lar ekle:

   ```tsx
   {/* Hero placeholder */}
   <section className="min-h-screen flex items-center justify-center bg-base-dark">
     <div className="text-center">
       <h1 className="font-display text-[40px] md:text-[64px] font-bold text-white neon-glow">
         DEJAVU
       </h1>
       <p className="font-body text-lg text-cream mt-4">Sac ve Sakal Tasarimi</p>
     </div>
   </section>

   <SectionDivider />

   {/* Hakkimizda placeholder */}
   <section id="hakkimizda" className="min-h-[70vh] bg-base-medium py-12 md:py-[var(--spacing-section)]">
     <div className="max-w-[var(--container-content)] mx-auto px-4 md:px-6">
       <h2 className="font-display text-[28px] md:text-[36px] font-bold text-cream">Hakkimizda</h2>
       <p className="font-body text-base text-cream/80 mt-4">Phase 2'de icerik gelecek...</p>
     </div>
   </section>

   <SectionDivider />

   {/* Hizmetler placeholder */}
   <section id="hizmetler" className="min-h-[70vh] bg-base-light py-12 md:py-[var(--spacing-section)]">
     <div className="max-w-[var(--container-content)] mx-auto px-4 md:px-6">
       <h2 className="font-display text-[28px] md:text-[36px] font-bold text-cream">Hizmetler</h2>
       <!-- RetroCard ornekleri -->
     </div>
   </section>

   <SectionDivider />

   {/* Iletisim placeholder */}
   <section id="iletisim" className="min-h-[70vh] bg-base-dark py-12 md:py-[var(--spacing-section)]">
     <div className="max-w-[var(--container-content)] mx-auto px-4 md:px-6">
       <h2 className="font-display text-[28px] md:text-[36px] font-bold text-cream">Iletisim</h2>
     </div>
   </section>

   {/* Copyright */}
   <div className="text-center py-4 bg-base-dark border-t border-gold/10">
     <p className="text-sm text-cream/50">{BUSINESS.copyright}</p>
   </div>
   ```

   Her section'da:
   - id attribute (scroll-spy icin)
   - min-h-[70vh] (scroll-spy test icin yeterli yukseklik)
   - Alternating backgrounds (base-dark -> base-medium -> base-light -> base-dark)
   - Section padding: py-12 mobil (48px ~ 3rem), md:py-[var(--spacing-section)] desktop (96px = 6rem)
   - Content max-width: max-w-[var(--container-content)] (1200px)
   - Content padding: px-4 mobil (16px), md:px-6 tablet+ (24px)

4. `src/app/layout.tsx` GUNCELLE — Navbar import ve eklenmesi:
   Task 1'de SmoothScrollProvider eklenmis layout'a simdi Navbar'i da ekle:
   ```tsx
   import { Navbar } from "@/components/layout/Navbar";
   // ... diger importlar

   <body className="font-body bg-base-dark text-cream min-h-screen">
     <AnimationProvider>
       <SmoothScrollProvider>
         <div className="grain-overlay">
           <Navbar />
           <main>{children}</main>
         </div>
       </SmoothScrollProvider>
     </AnimationProvider>
   </body>
   ```
   ONEMLI: Navbar, SmoothScrollProvider ICINDE — Lenis scroll event'lerini alabilmesi icin.
  </action>
  <verify>
    <automated>cd D:/nextJS/berber-sitesi && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/Navbar.tsx contains '"use client"' and "useScrollSpy" and "NAV_ITEMS" and "DEJAVU" and "neon-glow" and "motion" and "scrolled" and "mobileMenuOpen" and 'aria-label="Menu"' and "aria-expanded"
    - src/components/layout/Navbar.tsx contains "h-16" (64px desktop) and "h-12" (48px mobile)
    - src/components/layout/Navbar.tsx contains "bg-transparent" and "backdrop-blur" and "nav-indicator"
    - src/components/layout/MobileMenu.tsx contains '"use client"' and "AnimatePresence" and "motion" and 'aria-label="Menuyu kapat"' and "onClose"
    - src/components/layout/MobileMenu.tsx contains "RANDEVU AL" or "CTA_TEXT"
    - src/app/page.tsx contains 'id="hakkimizda"' and 'id="hizmetler"' and 'id="iletisim"'
    - src/app/page.tsx contains "DEJAVU" and "neon-glow" and "SectionDivider" and "--spacing-section" and "--container-content"
    - src/app/layout.tsx contains "Navbar" and "SmoothScrollProvider" and "AnimationProvider"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Navbar transparent->solid gecisi calisiyor, scroll-spy neon alt cizgi ile aktif bolumu gosteriyor, mobil menu tam ekran overlay aciliyor, Lenis desktop'ta smooth scroll sagliyor, ornek section'lar ile tam scroll deneyimi demo edilebilir durumda.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 1 tam deneyim kontrolu</name>
  <what-built>
    Next.js 15 proje kurulumu, vintage Turk berber design system (renk token'lari, font'lar, grain overlay, barber pole divider, retro card, neon glow), GSAP + Motion animasyon altyapisi, uc katmanli animasyon degradasyonu, Lenis smooth scroll (desktop), sticky navbar ile scroll-spy, tam ekran mobil menu.
  </what-built>
  <how-to-verify>
    1. `npm run dev` calistir, localhost:3000 ac
    2. DESKTOP kontrolu:
       - Sayfa koyu kahverengi arka plan ile yuklensin
       - "DEJAVU" basligi neon kirmizi glow ile gorunuyor mu?
       - Ince grain dokusu fark ediliyor mu?
       - Barber pole divider kirmizi-beyaz-mavi animasyonlu mu?
       - RetroCard altin kose suslemeleri gorunuyor mu?
       - Playfair Display (basliklar) ve Inter (govde) fontlari dogru mu?
    3. NAVBAR kontrolu:
       - Sayfa ustunde navbar transparan mi?
       - Asagi scroll edince koyu arka plan + blur aktif mi?
       - "DEJAVU" logosu neon glow ile gorunuyor mu?
       - Scroll ederken dogru bolum neon kirmizi alt cizgi ile isaretleniyor mu?
       - "RANDEVU AL" butonu WhatsApp linkine mi gidiyor?
    4. SMOOTH SCROLL kontrolu:
       - Desktop'ta scroll yumusak (Lenis) mi?
       - Navbar linkine tiklaninca ilgili bolume kayiyor mu?
    5. MOBIL kontrolu (DevTools ile 375px):
       - Hamburger ikonu gorunuyor mu?
       - Tiklaninca tam ekran menu aciliyor mu?
       - Menu'de tum linkler + Randevu Al butonu var mi?
       - Smooth scroll DEVRE DISI mi (native scroll)?
    6. ANIMASYON TIER kontrolu:
       - Demo bolumdeki GSAP fade-in ve Motion hover calisiyor mu?
       - Tier gostergesi desktop'ta "full" mu?
       - Mobil boyutta "reduced" mu?
       - DevTools > Rendering > prefers-reduced-motion acinsa "none" mu?
    7. ACCESSIBILITY:
       - Hamburger butonunda aria-label="Menu" var mi? (DevTools inspect)
       - Kapat butonunda aria-label="Menuyu kapat" var mi?
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` hatasiz tamamlaniyor
- `npx tsc --noEmit` TypeScript hatalari yok
- Phase 1 ROADMAP success criteria kontrolu:
  1. Running `npm run dev` serves a Next.js 15 App Router site with TypeScript and Tailwind CSS 4 working
  2. A test component demonstrates GSAP and Motion animations without hydration errors, with clear GSAP/Motion boundary
  3. The vintage barber design system is visible: dark base colors, red/neon accent variables, gold/cream accents, texture overlays, retro border styles
  4. The three-tier animation hook works: full effects on desktop, reduced on mobile, none with prefers-reduced-motion
  5. Lenis smooth scroll is active and navbar scrolls to section anchors with scroll-spy highlighting
</verification>

<success_criteria>
- Lenis smooth scroll desktop'ta aktif, GSAP ticker ile senkronize, mobilde devre disi
- Navbar transparent -> solid gecisi Motion ile calisiyor (hydration error yok)
- Scroll-spy neon kirmizi alt cizgi ile dogru bolumu isaretliyor
- Mobil menu tam ekran overlay, AnimatePresence ile enter/exit
- Tum section'lar alternating background ile scroll-spy demo icin hazir
- Phase 1'in 5 success criteria'si karsilanmis
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
